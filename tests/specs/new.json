[
  {
    "name": "start a new conversation from the menu",
    "steps": [
      { "send": { "callback": "conversation:new" }, "expect": [{ "method": "editMessageText", "payload": { "text": "Starting fresh. What would you like to explore?" } }] }
    ]
  }
]
