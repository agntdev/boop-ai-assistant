[
  {
    "name": "switch model from the menu",
    "steps": [
      { "send": { "callback": "model:show" }, "expect": [{ "method": "editMessageText", "payload": { "text": "You’re using Boop Balanced. Pick a style any time." } }] },
      { "send": { "callback": "model:set:focused" }, "expect": [{ "method": "editMessageText", "payload": { "text": "You’re now using Boop Focused." } }] }
    ]
  }
]
