[
  {
    "name": "list memories shows a helpful empty state",
    "steps": [
      { "send": { "callback": "memory:list" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No memories yet — tap Remember to add one." } }] }
    ]
  }
]
